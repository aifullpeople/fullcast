# Project Brief: <Project Name>

*Discovery stage output. Optional — skip straight to the PRD when the user already
arrives with full context.*

## Problem

<what pain motivates this project; who feels it; how they cope today without it>

## Audience hypothesis

<who this is for, in one or two sentences — the PRD's Target Audience section will
refine this into full personas>

## Value hypothesis

<what changes for the user if this exists; the core bet this project is making>

## Existing capabilities inventory

*Only when the repo is non-empty at discovery time — this is the OpenSpec-style "what
already exists" check, folded into discovery so the PM doesn't propose requirements
that ignore or duplicate what's already built. Read `context_project.md` first; if it
already has enough detail, this section can just point there instead of repeating it.*

<short bullet list of what the codebase already does, if anything>

## Open questions

<anything discovery surfaced that requirements-gathering (the PRD interview) still
needs to resolve>
